apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: ai-interview-assistant
  labels:
    app: ai-interview-assistant
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: ai-interview-assistant
    component: backend
